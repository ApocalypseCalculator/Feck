datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
}

model File {
    id   String @id
    name String @default("unknown")
    date Int
    size String
}

model CSRF {
    token     String @id
    generated Int
}
